@{
    ViewBag.Title = "新聞活動 News｜智匯創新 G-wise Design";
}

@section Styles {
    <link href="/css/news.css" rel="stylesheet" />
}

@section Scripts {
    <script src="/js/news.js"></script>
    <script>
        // 全局变量：详情页基础 URL（用于 openDetail）
        var newsDetailBase = '@Url.Action("Detail", "News")';
    </script>
}

<!-- HERO -->
<div class="page-hero">
    <div class="page-hero-bg" style="background-image:url('https://images.unsplash.com/photo-1504711434969-e33886168f5c?w=1600&q=80');"></div>
    <div class="page-hero-grad"></div>
    <div class="page-hero-content">
        <div class="eyebrow">
            <div class="eyebrow-line"></div>
            <span class="eyebrow-label">News &amp; Events</span>
        </div>
        <h1 class="page-title">新聞活動<br>News</h1>
    </div>
</div>

<!-- BREADCRUMB -->
<div class="breadcrumb">
    <a href="@Url.Action("Index", "Home")">首頁</a>
    <span>›</span>
    <span style="color:#fff" id="bcText">@(ViewBag.CurrentCategoryName ?? "新聞消息")</span>
</div>

<!-- TAB NAV - 从后端数据绑定 -->
<div class="tab-nav">
    <a href="@Url.Action("Index", "News")" class="tab-btn @(ViewBag.CurrentCategoryId == 0 ? "active" : "")">全部新聞</a>
    @if (ViewBag.CategoryList != null)
    {
        var categories = ViewBag.CategoryList as List<Academy.Models.Category> ?? new List<Academy.Models.Category>();
        foreach (var cat in categories)
        {
            <a href="@Url.Action("Index", "News", new { categoryId = cat.Id })" class="tab-btn @(ViewBag.CurrentCategoryId == cat.Id ? "active" : "")">@cat.Name</a>
        }
    }
</div>

<!-- NEWS LIST -->
<div id="panelNews">
    @if (ViewBag.NewsList == null || ((IEnumerable<dynamic>)ViewBag.NewsList).Count() == 0)
    {
        <div style="text-align:center;padding:100px;color:var(--gray);">暫無新聞</div>
    }
    else
    {
        var newsList = ViewBag.NewsList as List<Academy.Models.News> ?? new List<Academy.Models.News>();
        var featured = newsList.FirstOrDefault();
        
        <div class="news-wrap">
            @if (featured != null)
            {
                <div class="news-featured" onclick="openDetail(@featured.ID)">
                    <div class="news-featured-img">
                        <img src="@(featured.ImagePath ?? featured.ImagePath)" alt="@featured.Title" loading="lazy">
                    </div>
                    <div class="news-featured-body">
                        <div class="news-featured-tag">@(featured.Title ?? "")</div>
                        <div class="news-featured-date">@(featured.CDate)</div>
                        <p class="news-featured-text">@featured.Note</p>
                        <a href="@Url.Action("Detail", "News", new { id = featured.ID })" class="news-read-more" >閱讀更多 →</a>
                    </div>
                </div>
            }

            @if (newsList.Count > 1)
            {
                <div class="news-grid">
                    @for (int i = 1; i < newsList.Count; i++)
                    {
                        var item = newsList[i];
                        <div class="news-card" onclick="openDetail(@item.ID)">
                            <div class="news-card-img">
                                <img src="@(item.ImagePath ?? item.ImagePath)" alt="@item.Title" loading="lazy">
                            </div>
                            <div class="news-card-body">
                                <div class="news-card-tag">@(item.Title ?? "")</div>
                                <div class="news-card-date">@(item.CDate)</div>
                                <p class="news-card-text">@item.Note</p>
                                <a href="@Url.Action("Detail", "News", new { id = item.ID })">閱讀更多 →</a>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>
    }
</div>

<!-- HIRING TAB -->
<div id="panelHiring" style="display:none;">
    <div class="hiring-wrap">
        <div class="hiring-section">
            <h3>企業精神</h3>
            <p>針對不同市場需求，為客戶創造最高品牌價值產品，是智匯創新的企業精神指標。智匯創新本著為客戶創造最高價值的心態做設計，而這些核心的能力，須從人才出發，結合眾人智慧，進而設計出具備外觀優美、品質優良的產品。</p>
        </div>

        <div class="hiring-section">
            <h3>企業文化</h3>
            <p>智匯創新特別著重在人才的專業素質與人格的培育，開放式管理風格，注重自我管理與創新精神。</p>
        </div>

        <div class="hiring-section">
            <h3>挑戰與機會</h3>
            <p>智匯創新不斷從事計畫性產品設計，從產品概念、產品外觀設計、機構設計、產品模型都是本公司的專業領域範圍，我們誠摯歡迎可塑性高且自我期許未來成為全方位人才的你加入我們行列。</p>
        </div>

        <div class="hiring-info-grid">
            <div class="hiring-info-card">
                <h4>工作時間</h4>
                <p>周休二日<br>08：30 — 17：30<br>午休時間一小時</p>
            </div>
            <div class="hiring-info-card">
                <h4>福利制度</h4>
                <p>年終固定提撥 8% 分紅<br>三節禮品<br>完善勞健保</p>
            </div>
            <div class="hiring-info-card">
                <h4>應徵方式</h4>
                <p>請下載履歷表格，填妥後 e-mail 至：<br><a href="mailto:service@g-wise.com">service@g-wise.com</a></p>
            </div>
            <div class="hiring-info-card">
                <h4>公司地址</h4>
                <p>桃園市桃園區經國路9號3樓之2<br>TEL：(03)358-2500</p>
            </div>
        </div>

        <a href="mailto:service@g-wise.com" class="hiring-cta">
            立即投遞履歷
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7" /></svg>
        </a>
    </div>
</div>

<!-- CTA BAR（已在 Layout 中，可省略） -->